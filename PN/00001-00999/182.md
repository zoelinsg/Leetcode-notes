# LeetCode

## [182. Duplicate Emails](https://leetcode.com/problems/duplicate-emails/)

### MySQL

```sql
SELECT email
FROM Person
GROUP BY email
HAVING COUNT(*) > 1;
```

---

### MSSQL

```sql
SELECT email
FROM Person
GROUP BY email
HAVING COUNT(*) > 1;
```